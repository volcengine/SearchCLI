# {{PROJECT_NAME}}

Generated by `vs project create`.

## Commands

```bash
npm install
npm run dev
npm run build
```

The browser app reads the features selected by `vs project create --features` from `/api/config`. Project IDs and credentials are kept on the API side in `apps/api/src/env.ts`. Generated credentials are plain text; for deployments, prefer overriding them with runtime environment variables.

Runtime authentication overrides:

- `VIKING_API_KEY` for API key authentication
- `VIKING_AK`, `VIKING_SK`, and optionally `VIKING_REGION` for AK/SK authentication

The API exposes only the selected feature routes and returns HTTP 404 for disabled features:

- `POST /api/chat` with `{ "message": "question", "sessionId": "optional-session-id" }`, returning a `text/event-stream` response
- `POST /api/search` with `{ "query": "keyword", "page": 1 }`, `{ "image": { "base64": "data:image/png;base64,..." }, "page": 1 }`, or both `query` and `image`
- `POST /api/query-recommendation`, returning recommended search queries in `recommendation_queries`
- `POST /api/recommend`, returning up to 20 recommendation results

Chat responses are streamed as SSE events: `session`, `chunk`, `error`, and `done`.
Successful search responses include pagination metadata from the template API.
Search query recommendation responses are returned directly from `@volcengine/search-node`.
Recommendation responses are returned directly from `@volcengine/search-node`.

Deploy through the CLI with an explicit provider:

```bash
vs project deploy --provider cloudflare
```
